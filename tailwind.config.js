const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    extend: {
      colors: {
        teal: colors.teal,
        cyan: colors.cyan,
        sky: colors.sky,
        gray: colors.gray,
        card: '#ffffff',
        bord: '#f3f4f6',
        backg: '#f7f8ff',
        white: colors.white,
      },
    },
  },
  variants: {},
  plugins: [],
}
